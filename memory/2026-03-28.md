

---

## 下午工作记录（13:30 左右）

### 1. Novel Shelf 小说管理系统 - Bug 修复完成

**修复了 9 个 Bug：**
- #5 设置页面邮箱为空 → 显示用户名+显示名称
- #1 文案口语化 → "选择作品开始创作"
- #2 按钮层次混乱 → 主次样式区分
- #4 章节标题重复 → 统一"第X章 标题"
- #3 统计数字简陋 → 徽章式展示
- #6 禁用按钮突兀 → 透明边框样式
- #7 工具区太挤 → 默认只展开章节导航
- #8 时间格式不统一 → YYYY-MM-DD HH:MM
- #9 缺少空状态 → 📝📋🎭🌍💡🔗+引导

**Commit:** 
- `762706a` fix: 修复高/中优先级 Bug
- `98cd5b2` fix: 修复低优先级 UI/UX 问题

**仓库:** OldYuTou/novel

---

### 2. STL 查看器方向调整

- 已修复线框与模型重合问题
- 添加了方向控制面板（X/Y/Z滑块+预设）
- 运行目录: `/root/stl-viewer/`
- 端口: 4000

等待用户验证。

---

### 3. Dishes 家庭食材管理系统 - 智能食谱功能

**新增功能：**
- `/api/recipes` API - 根据冰箱食材生成食谱
- `/recipes` 页面 - 展示推荐食谱
- 优先使用快过期食材（≤3天）
- 食材对比：✅已有 vs ❌缺少
- 冰箱页面添加智能食谱入口

**Commit:** `ecab780` feat: 实现智能食谱生成功能 (#5)

**仓库:** OldYuTou/dishes

---

### 4. 新流程确认

**汇报方式变更：**
- 完成任务后直接群里 @鱼头 汇报进度
- 不再等芋头中转
- 模板：`@余腾飞 [项目名] 完成：- [任务] ✅`

---

## 下午工作记录（15:40 左右）

### 5. STL 查看器 - 大模型显示修复

**问题：**
1:1 桌子模型（尺寸过大）导致显示"闪一下就消失"

**修复：**
- 自动缩放：模型 >500 单位时自动缩放
- 相机距离优化：确保距离 = max(计算值, 模型尺寸×1.5)
- 动态裁剪平面：根据模型大小调整 near/far
- 更新缓存版本至 v=4

**运行目录:** `/root/stl-viewer/` (端口 4000)

---

### 6. self-improving-agent Skill 安装

**位置:** `/root/.openclaw/workspace-cc/skills/self-improving-agent/`

**功能：**
- 记录学习/修正到 `.learnings/LEARNINGS.md`
- 记录错误到 `.learnings/ERRORS.md`
- 记录功能请求到 `.learnings/FEATURE_REQUESTS.md`

---

*记录时间: 2026-03-28 13:33 / 更新: 2026-03-28 16:14*

---

## 晚间工作记录（18:00 - 20:00）

### 7. STL 查看器 - 多项功能完善

#### 7.1 坐标轴跟随旋转修复
- 坐标轴现在会跟随模型一起旋转
- 蓝绿红三色轴与模型旋转同步

#### 7.2 坐标轴图例
- 在信息面板新增图例区域：
  - 🔴 红色 = X轴（左右）
  - 🟢 绿色 = Y轴（上下）
  - 🔵 蓝色 = Z轴（前后）

#### 7.3 初始旋转设置
- 默认旋转：X轴 -90°, Z轴 -90°
- 预设选择器改为"自定义 (X-90°, Z-90°)"
- 重置按钮同步更新

#### 7.4 面板隐藏功能
- 右下角新增 📋 切换按钮
- 默认隐藏信息面板（沉浸式体验）
- 点击按钮可显示/隐藏面板

#### 7.5 下载功能
- 顶部控制栏新增 ⬇️ 下载按钮
- 带验证码保护（验证码：`REDACTED_TOKEN_SHORT`）
- 验证通过后自动下载当前 STL 文件

**运行目录:** `/root/stl-viewer/` (端口 4000)

---

*记录时间: 2026-03-28 21:08*

---

## 晚间工作记录（21:00 - 22:00）

### 8. STL 查看器 - 登录鉴权功能

**功能：**
- 访问网站需要先登录
- 账号: `youngtaro`
- 密码: `stl2026`
- Cookie 永久保存（10年）
- 回车键快捷登录

**实现：**
- 后端：`/api/login`, `/api/check-auth`, `/api/logout`
- 前端：登录页面 + Cookie 管理
- 静态文件也需要鉴权

---

### 9. STL 查看器 - 分享链接功能

**功能：**
- 生成一次性分享链接（只能查看一次）
- 链接打开是只读的 3D 模型展示页面
- 可以旋转、缩放查看模型
- **不能下载**
- 30分钟过期

**技术实现：**
- `/api/share` API 生成令牌
- `/share/:token` 返回展示页面
- 独立模板 `share-template.html`
- 保留分享时的旋转角度
- 自动缩放大模型
- 显示坐标轴和网格

**按钮位置：**
- 下载按钮旁边新增 🔗 分享按钮
- 生成后自动复制到剪贴板
- 弹窗显示完整链接

---

### 10. 问题修复记录

1. **登录失败** → 强制刷新缓存（版本号更新）
2. **下载验证码取消** → 登录后即可直接下载
3. **分享链接生成失败** → 修复路径验证逻辑（支持文件名）
4. **分享页面空白** → 修复模板缺少 `canvas-container` 元素

---

*记录时间: 2026-03-28 22:11*
